version: 1

frontend:
  phases:
    preBuild:
      commands:
        - pwd
        - ls
        - cat package.json
        - npm install

    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: dist
    files:
      - '**/*'