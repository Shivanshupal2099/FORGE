# TODO - Homepage post centering + SurveyTaker redesign

- [x] Update `Frontend/FORGE/src/App.css` to center the surveys (“post”) section on the homepage.
- [x] Remove/merge duplicate `.home-surveys-section` declarations to avoid conflicting behavior.
- [x] Redesign `SurveyTaker` modal styling in `App.css`: improve spacing, typography, option cards, and selected state.
- [x] Ensure CSS class names used by `SurveyTaker.jsx` have consistent styling (e.g., `survey-taker__question-title`, `survey-taker__actions`, etc.).
- [x] Quick sanity check by running the dev server / build command (if available) or at least ensure no CSS syntax errors.

